/*module.exports = () => {
  console.log('Welcome to the outside!')
}*/

/*let fs = require('fs');
let path = require('path');
let md = fs.readdirSync(.filter(fn => fn.endsWith('.md'));
fs.readdirSync(md, {encoding: 'UTF-8'}, function(err, data){
  if(err){
    console.log('error 123');
  }else{
    console.log('datos leidos');
    let a = data.split("crear");
    let b = Object.values(a);
    
    console.log(md);
  }
})*/
/*
let path = require('path');
let fs = require('fs');
let mdFiles =[ ]
let md = fs.readdirSync(process.cwd()).filter(fn => fn.endsWith('.md'))
console.log(md);
 mdFiles = mdFiles.concat(this.findeFile(filePath)).path.extname((f) => f === '.md');
console.log(mdFiles)
fs.readFile(mdFiles, {encoding: 'UTF-8'}, function(err, data){
  if(err){
    console.log('error 123');
  }else{
    console.log('datos leidos');
    let a = data
    
    console.log(a);
  }
})
*/


/*let fs = require('fs');
const path = require('path');
fs.readdirSync('readme.md'), {encoding: 'UTF-8'}, function(err, data){
  if(err){
    console.log('error 123');
  }else{
    console.log('datos leidos');
    console.log(data)
  }
}*/

